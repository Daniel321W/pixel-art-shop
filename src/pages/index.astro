---
import Layout from '../layouts/Layout.astro';
import { products } from '../data/products.js';

// Tu możesz filtrować produkty jeśli chcesz
---

<Layout title="Pixel Art Shop - Unikalne ozdoby z koralików">
  
  <nav class="bg-white shadow-sm sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
      <div class="text-2xl font-bold text-indigo-600">Pixel<span class="text-gray-800">Shop</span></div>
      <div class="space-x-4">
        <a href="#galeria" class="text-gray-600 hover:text-indigo-600 font-medium">Galeria</a>
        <a href="#zamow" class="bg-indigo-600 text-white px-4 py-2 rounded-full hover:bg-indigo-700 transition">Zamów projekt</a>
      </div>
    </div>
  </nav>

  <header class="bg-indigo-50 py-16 md:py-24">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-6xl font-extrabold text-gray-900 mb-6">
        Twoje ulubione postacie <br/> <span class="text-indigo-600">w wersji Pixel Art</span>
      </h1>
      <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
        Ręcznie robione ozdoby z koralików. Breloki, magnesy, podstawki i figurki. 
        Wybierz gotowy wzór lub zamów coś unikalnego!
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#galeria" class="px-8 py-3 bg-indigo-600 text-white font-bold rounded-lg hover:bg-indigo-700 transition shadow-lg text-center">
          Zobacz Gotowe Wzory
        </a>
        <a href="https://www.vinted.pl/member/91601749" target="_blank" class="px-8 py-3 bg-white text-indigo-600 font-bold rounded-lg border border-indigo-200 hover:bg-gray-50 transition text-center">
          Mój Profil Vinted
        </a>
      </div>
    </div>
  </header>

  <section id="galeria" class="py-16 bg-white">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Dostępne Wzory</h2>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        
        {products.map((product) => (
          <div class="group bg-white border border-gray-100 rounded-2xl overflow-hidden shadow-sm hover:shadow-xl transition duration-300">
            <div class="aspect-square bg-gray-100 overflow-hidden relative">
              <img 
                src={product.image} 
                alt={product.title} 
                class="w-full h-full object-cover group-hover:scale-105 transition duration-500"
              />
              <span class="absolute top-3 right-3 bg-white/90 backdrop-blur-sm px-3 py-1 rounded-full text-sm font-bold shadow-sm">
                {product.price}
              </span>
            </div>
            
            <div class="p-6">
              <div class="text-xs text-indigo-500 font-bold uppercase tracking-wide mb-1">{product.category}</div>
              <h3 class="text-xl font-bold text-gray-900 mb-4">{product.title}</h3>
              
              <div class="flex gap-2">
                <a href={product.vintedLink} target="_blank" class="flex-1 bg-teal-500 hover:bg-teal-600 text-white text-center py-2 rounded-lg font-medium transition flex items-center justify-center gap-2">
                   <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
                   Kup na Vinted
                </a>
              </div>
            </div>
          </div>
        ))}

      </div>
    </div>
  </section>

  <section id="zamow" class="py-16 bg-gray-900 text-white">
    <div class="max-w-4xl mx-auto px-4 text-center mb-10">
      <h2 class="text-3xl font-bold mb-4">Masz własny pomysł?</h2>
      <p class="text-gray-400">Wyślij mi zdjęcie lub opisz co chcesz. Wycenię to dla Ciebie!</p>
    </div>

    <div class="max-w-xl mx-auto px-4 bg-gray-800 p-8 rounded-2xl shadow-2xl">
      <form action="https://formspree.io/f/mjgeaegg" method="POST" class="space-y-4">
        
        <div>
          <label class="block text-sm font-medium text-gray-300 mb-1">Twój Email</label>
          <input type="email" name="email" required placeholder="adres@email.com" 
            class="w-full px-4 py-3 rounded-lg bg-gray-700 border border-gray-600 text-white focus:outline-none focus:border-indigo-500 transition" />
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-300 mb-1">Co chcesz zamówić?</label>
          <textarea name="message" rows="4" required placeholder="Np. Chcę Mario, ale w niebieskim stroju, wielkość około 15cm..." 
            class="w-full px-4 py-3 rounded-lg bg-gray-700 border border-gray-600 text-white focus:outline-none focus:border-indigo-500 transition"></textarea>
        </div>

        <div>
           <label class="block text-sm font-medium text-gray-300 mb-1">Link do grafiki (opcjonalnie)</label>
           <input type="text" name="link" placeholder="Link do zdjęcia wzoru"
            class="w-full px-4 py-3 rounded-lg bg-gray-700 border border-gray-600 text-white focus:outline-none focus:border-indigo-500 transition" />
        </div>

        <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-500 text-white font-bold py-3 rounded-lg transition transform hover:scale-[1.02]">
          Wyślij do darmowej wyceny
        </button>
      </form>
    </div>
  </section>

  <footer class="bg-gray-900 border-t border-gray-800 py-8 text-center text-gray-500 text-sm">
    <p>&copy; 2024 PixelShop. Wszystkie prawa zastrzeżone.</p>
  </footer>

</Layout>